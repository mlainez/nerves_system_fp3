# boardid.config

# Read the Qualcomm SoC serial number from sysfs. The mainline driver
# exposes it at /sys/devices/soc0/serial_number; fall back to cpuinfo
# if not available (e.g. recovery boots without soc0 bound).
-b binfile -f /sys/devices/soc0/serial_number -n 8 || true
-b cpuinfo
